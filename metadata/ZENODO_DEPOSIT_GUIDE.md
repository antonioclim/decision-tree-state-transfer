# Zenodo deposit metadata guide

The final manual-deposit metadata are stored in `zenodo-deposit.json`. The authorised extended archive is deposited separately from the GitHub source release so that the raw Study 1 and Study 2 evidence remains bound to the exact archival candidate. After publication, add the assigned DOI to the article Data Availability Statement and to a subsequent documentation-only repository update.
